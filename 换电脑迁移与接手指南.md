# 元宝爱财 · 换电脑迁移与接手指南

> 目的：把「元宝爱财」盯盘工具连同 WorkBuddy 项目记忆，完整搬到其他电脑，让新电脑的 WorkBuddy 无缝接手继续改。
> 适用版本：主文件 `元宝爱财-摸鱼致富.html`（2026-08-07 版：金十数据接入 + 阿里云部署 + server.js 监听 0.0.0.0）
> 最后更新：2026-08-07

---

## 一、项目归纳总结

| 项 | 内容 |
|---|---|
| 是什么 | 猫咪主题 A股 / 港股 / 现货黄金 桌面 + 手机盯盘小窗，应用名「元宝爱财」 |
| 形态 | **单文件 HTML**（约 2.5MB），完全自包含（7 张猫图全部 Base64 内联） |
| 主文件 | `元宝爱财-摸鱼致富.html` |
| 启动 | 双击 `启动-置顶.bat`（PROXY 模式 + 系统级置顶）或 `启动.bat`（PROXY，无置顶） |
| 桌面样式 | 宽 356px 小窗；顶栏固定 + 内容滚动；自由缩放靠浏览器原生窗口 |
| 手机样式 | 自适应满宽：≤480px 弹性列 + 代码紧跟名称；≤360px 再缩一档 |
| 数据列 | 四列：名称 \| 价格 \| 涨跌值 \| 涨跌幅% |
| 颜色 | 分组标题深棕 #2D2014；副文本/代码灰 #888888；涨红跌绿 |
| 额外功能 | 顶栏「摸鱼模式」按钮：弹独立小窗，主窗数据收起 |
| 窗口置顶 | `启动-置顶.bat` 调 `置顶.ps1`（Win API HWND_TOPMOST）系统级 always-on-top |
| 数据源 | 腾讯行情 qt.gtimg.cn、金十 MCP（/api/jin10/*：国际金价 XAUUSD / 国内金价 ZHJCJ 招行积存金 / 快讯 / 新闻 / 日历）、东方财富 push2/push2his（**当前本机网络不可达，已降级**）、腾讯搜索 smartbox、腾讯分时/日周月K |
| 国内金价 | 主源 = 金十 ZHJCJ 招行积存金 → 招行 SGE Au99.99 兜底 → 东财/腾讯 ETF 反推兜底 |
| 代理 | `server.js` 端口 18765，监听 `0.0.0.0`（支持局域网/公网）；仅用 Node 内置模块，**无需 node_modules** |
| 云端部署 | 阿里云 ECS `120.24.240.129`，宝塔 `/yuanbao/` + Supervisor 守护 + Nginx 反代（手机脱离 WiFi 也能看） |

---

## 二、文件清单：哪些一定带走

### ✅ 必需（最小可运行集）
| 文件/目录 | 说明 |
|---|---|
| `元宝爱财-摸鱼致富.html` | **核心**，自包含单文件，这一个就能跑 |
| `server.js` | 数据代理（端口 18765，监听 0.0.0.0）。**仅用 Node 内置模块（http/https/url/fs/path），无需 npm install** |
| `启动.bat` `启动-置顶.bat` `启动-摸鱼小窗.bat` `启动-主窗口.bat` | 启动器（PROXY 模式：自动起 server.js + Chrome `--app` 小窗） |
| `置顶.ps1` | 系统级置顶脚本（被 启动-置顶.bat 调用） |
| `.workbuddy/` | **项目记忆**（含 `MEMORY.md` + 每日日志），让新电脑 WorkBuddy 接手 |

> ⚠️ `.workbuddy` 是隐藏目录，拷贝时务必勾选「显示隐藏文件」，否则记忆会丢。

### 🟡 阿里云部署相关（在服务器上，本地了解即可，不必带走运行）
| 文件 | 说明 |
|---|---|
| `run.sh` `start.sh` | 服务器端启动脚本（run.sh 给 Supervisor 用） |
| `supervisor-yuanbao.ini` | 宝塔进程守护配置 |
| `setup-nginx.sh` | 一键配置 Nginx 反代 `/yuanbao/` → `127.0.0.1:18765` |
| `元宝爱财-阿里云部署包.zip` | 部署用打包（含最新 HTML/server/启动脚本） |
| `宝塔部署说明.md` `部署到阿里云.md` | 部署手册 |

### ❌ 可删（不影响运行，省空间）
| 文件 | 说明 |
|---|---|
| `node_modules/`（约 484MB） | 纯 Electron 残留，**完全用不到**，别带 |
| `main.js` `renderer.js` `preload.js` `index.html`(根目录的旧版) `config.json` `datafeed.js` `test-datafeed.js` `package.json` `package-lock.json` | 07-07 Electron 桌面版残留 |
| `上传版/` | 旧副本 |
| `yuanbao.jpg` `元宝.jpg` `cat-*.png` `cat-footer.jpg` `moyu-cat.webp` | 图片源素材，已内联进 HTML |
| `2000` `300` `nul` `server.log` `test-screenshots/` `迁移.png` | 杂项/日志/截图 |

> 建议：带走「必需」六项即可。归档包 `元宝爱财-归档.zip` 已裁剪（不含 node_modules / 上传版 / 图片源素材）。

---

## 三、换电脑步骤（5 分钟）

1. **拷贝**：把整个 `摸鱼理财` 文件夹（**含隐藏的 `.workbuddy`**）拷到新电脑任意位置（U盘 / 网盘 / 微信文件传输助手都行）。或解压归档包 `元宝爱财-归档.zip`。
2. **装环境**：新电脑安装 **WorkBuddy** + **Google Chrome**（小窗依赖 Chrome `--app`）+ **Node.js ≥ 18**（强烈推荐，用于行情代理兜底；不装则 bat 降级 file://，金价走浏览器侧兜底仍可用）。
3. **登录账号**：用**同一 WorkBuddy 账号**登录。个人背景、专家包、自定义 Skill（用户级）由服务端自动同步；**项目记忆随文件夹走，无需手动拷**。
4. **运行**：双击 `启动-置顶.bat` → 自动起 server.js + 弹无边框盯盘小窗（需联网拉行情）。
5. **接手**：用 WorkBuddy 打开 `摸鱼理财` 项目目录，它会自动读 `.workbuddy/memory/MEMORY.md`，瞬间恢复全部上下文。

---

## 四、WorkBuddy 记忆迁移

| 记忆类型 | 位置 | 换电脑怎么办 |
|---|---|---|
| **项目记忆**（设计参数/偏好/踩坑） | `摸鱼理财/.workbuddy/memory/` | ✅ 随文件夹带走，无需额外操作 |
| **个人背景**（学历/持仓/职业等） | 服务端自动注入 | ✅ 登录同一账号自动恢复 |
| **用户级 Skill** | `C:\Users\<你>\.workbuddy\skills\` | 🟡 如需保留，把整个 `skills` 目录拷到新电脑同名位置 |

> 本项目无用户级 `~/.workbuddy/MEMORY.md`（个人背景走服务端注入），所以**只需保证项目文件夹完整 + 登录同账号**。

---

## 五、阿里云部署（换电脑不影响）

手机端盯盘跑在云端 `120.24.240.129`，**换电脑后照常可用，无需重新部署**。

- 宝塔目录 `/www/wwwroot/120.24.240.129/yuanbao/`（含 index.html + server.js）
- 进程守护：**Supervisor** 守护 `yuanbao-server`（RUNNING）
- Nginx 反代：`location /yuanbao/ { proxy_pass http://127.0.0.1:18765/; }`
- 访问：`http://120.24.240.129/yuanbao/`

**只有当你改了代码要重新部署时**，才需要：本地改完 `元宝爱财-摸鱼致富.html` / `server.js` → 重新打包部署包 → 传服务器覆盖 → 重启 Supervisor。

---

## 六、Nginx 反代配置（服务器用，重装服务器时备用）

在宝塔「网站 → 120.24.240.129 → 设置 → 配置文件」的 `server {}` 内、`access_log` 之前插入：

```nginx
location /yuanbao/ {
    proxy_pass http://127.0.0.1:18765/;
    proxy_set_header Host $http_host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
}
```

> `proxy_pass` 末尾斜杠必须有（strip `/yuanbao/` 前缀，server.js 收干净 `/api/...`）。
> 保存后 `nginx -t && nginx -s reload`。验证：`curl http://120.24.240.129/yuanbao/api/detail?secid=0.002384` 返回 JSON 即成功。

---

## 七、换电脑后小坑速查

| 现象 | 原因 | 解决 |
|---|---|---|
| 双击 bat 没反应 | 路径/文件名错 | 确保 bat 与 html 同目录 |
| 打开后有滚动条/内容被裁 | 窗口 < 356px | 改 bat 里 `--window-size` |
| 用 Edge 打开（非无边框） | 没装 Chrome | 装 Chrome，bat 优先用它 |
| 行情空白 | 网络拦截 | 确保装了 Node.js（PROXY 多源兜底） |
| 国内金价不准 | 没装 Node.js，走 file:// 降级 | 装 Node.js 后双击 bat 自动切 PROXY |
| 点「摸鱼模式」没弹窗 | 浏览器拦截弹窗 | 允许本站弹窗 |
| 猫图不显示 | 用了删图副本 | 确认用单文件 `元宝爱财-摸鱼致富.html` |
| 置顶不生效 | 没用 启动-置顶.bat | 系统级置顶仅该 bat 生效 |
| 金十数据缺失 | server.js 没起 / token 失效 | 确认 `yuanbao-server` RUNNING；公网用环境变量 `JIN10_TOKEN` 覆盖 |

---

## 八、给 WorkBuddy 的接手提示（直接复制）

> 「先读 `F:/摸鱼理财/.workbuddy/memory/MEMORY.md`，这是元宝爱财盯盘工具的定稿上下文。现在我要改：___（你的需求）___。只动我说的部分，其他保持原样。」

---

*生成日期：2026-07-24 ｜ 2026-08-04 更新 PROXY 版 ｜ 2026-08-07 更新（金十接入 + 阿里云部署 + server.js 0.0.0.0 + Nginx 反代）｜ 由 WorkBuddy 归纳，随项目一起迁移。*
