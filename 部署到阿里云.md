# 元宝爱财 盯盘服务 - 部署到阿里云（宝塔场景）

> 你当前已用宝塔把前端托管在 `/www/wwwroot/120.24.240.129/yuanbao/`，访问 `http://120.24.240.129/yuanbao/`。
> 详情页数据需要后端 `server.js`（默认 18765 端口）。**推荐用宝塔 Nginx 反向代理**，让 `/yuanbao/` 下的请求全部交给 server.js，18765 端口不必公网暴露、也不涉及跨域。

---

## 一、要上传的文件（放同一目录：`/www/wwwroot/120.24.240.129/yuanbao/`）

| 文件 | 作用 |
|------|------|
| `index.html` | 前端单文件（即原 `元宝爱财-摸鱼致富.html`，已重命名） |
| `server.js` | 数据代理（监听 0.0.0.0:18765，读取同目录 HTML） |
| `start.sh` | 一键启动脚本 |

> `server.js` 优先读同目录的 `index.html`，找不到才读 `元宝爱财-摸鱼致富.html`，**三个文件必须放同一目录**。

---

## 二、服务器端操作（SSH 进去）

```bash
cd /www/wwwroot/120.24.240.129/yuanbao
ls -la                      # 确认 index.html / server.js / start.sh 都在

node -v                     # 确认 Node >= 18，没有先装

chmod +x start.sh
JIN10_TOKEN=你的金十key ./start.sh
# 不带 token 也行（server.js 内置硬编码 key，但公网建议用环境变量覆盖）

# 验证本机服务
curl -s -m 5 http://127.0.0.1:18765/ -o /dev/null -w "%{http_code}\n"
# 期望输出 200
```

---

## 三、宝塔配置反向代理（关键！否则详情页 404）

目标：让 `http://120.24.240.129/yuanbao/` 整站由 server.js 提供（HTML + API 同源，无需公网放行 18765）。

**方式 A：宝塔 GUI（推荐）**
1. 宝塔 → 网站 → 你的站点 `120.24.240.129` → **反向代理** → 添加反向代理
2. 填写：
   - 代理名称：`yuanbao`
   - 目标 URL：`http://127.0.0.1:18765`
   - **代理目录**：`/yuanbao/`（只代理这个子目录）
   - 发送域名：`120.24.240.129`
   - 内容替换：可开启（把 `127.0.0.1:18765` 替换为 `120.24.240.129`，避免页面内绝对链接错乱）
3. 保存。

**方式 B：直接改 Nginx 配置**
宝塔 → 网站 → 设置 → 配置文件，在 `server { ... }` 内、`location / { ... }` 之前加：

```nginx
location /yuanbao/ {
    proxy_pass http://127.0.0.1:18765/;   # 末尾斜杠：把 /yuanbao/xxx 重写为 /xxx 发给后端
    proxy_set_header Host 120.24.240.129;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
}
```

保存后点「重载配置」。

**验证：**
- 浏览器打开 `http://120.24.240.129/yuanbao/` → 应显示行情主页
- 点任意股票 → 详情弹窗分时 / 日K / 周K / 月K 应正常显示
- 若仍 404，检查 server.js 是否真的在跑（`ps -ef | grep server.js`），以及反代配置是否生效

---

## 四、为什么不直接用 18765 端口？

也可以，但需额外两步（见下方「备选模式」），而且 18765 直接公网暴露会让金十 token 所在的后端更易被扫。
反向代理模式下，18765 只在服务器内网 `127.0.0.1` 通信，外部只看到 80 端口的 `/yuanbao/`，更安全也更简单。

### 备选模式：直接访问 18765（无宝塔时）
1. 阿里云安全组入方向放行 **TCP 18765/18765**（授权 `0.0.0.0/0`）
2. 服务器系统防火墙放行 18765
3. 手机直接访问 `http://120.24.240.129:18765/`
> 此模式前端接口仍走同源（页面与 API 都在 18765），无需反代，但需公网放行端口。

---

## 五、资金流 / 五日分时说明

依赖东方财富接口，若服务器侧也连不上东财（socket hang up），会显示「东方财富接口维护中」，属预期降级，东财恢复后自动可用。其他图表（基础报价 / 分时 / 日K 周K 月K）不受影响。

## 六、开机自启 + 崩溃重启（可选）

### pm2
```bash
npm install -g pm2
cd /www/wwwroot/120.24.240.129/yuanbao
pm2 start server.js --name yuanbao
pm2 save && pm2 startup
```

### systemd
`/etc/systemd/system/yuanbao.service`:
```ini
[Unit]
Description=Yuanbao Stock Proxy
After=network.target

[Service]
WorkingDirectory=/www/wwwroot/120.24.240.129/yuanbao
ExecStart=/usr/bin/node /www/wwwroot/120.24.240.129/yuanbao/server.js
Environment=JIN10_TOKEN=你的金十key
Restart=always
User=root

[Install]
WantedBy=multi-user.target
```
```bash
systemctl daemon-reload && systemctl enable --now yuanbao
```

## 七、安全提醒

- ⚠️ 金十 token 硬编码在 `server.js`（约第 835 行）。公网暴露后**务必用 `JIN10_TOKEN=xxx` 环境变量覆盖**，不要把硬编码 key 提交到公开仓库。
- 反向代理模式下 18765 不需公网暴露，token 更安全。
- 备案完成后若改用域名根路径（如 `http://域名/`），把反代目录从 `/yuanbao/` 改为 `/` 即可，前端 `apiBase()` 会自动适配。
