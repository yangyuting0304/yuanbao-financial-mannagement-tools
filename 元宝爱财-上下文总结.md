# 元宝爱财 · 上下文总结文档（2026-08-12 ~ 09-16 迭代归档）

> 用途：快速恢复跨会话上下文。**新会话先读本文件**，再按需读 `.workbuddy/memory/` 下的日志与 `MEMORY.md`。
> 本文件是"本次迭代总结"，长期权威上下文见 `.workbuddy/memory/MEMORY.md`。
> **已上传 GitHub**：https://github.com/yangyuting0304/yuanbao-financial-mannagement-tools（主分支 main）

---

## 0. 一句话概况

猫咪主题 A股/港股/黄金 盯盘小窗，单文件 HTML（`元宝爱财-摸鱼致富.html` ~3.3MB），本地/云端双模式运行。8-12~8-13 完成扁平化 UI 改造 + 价格提醒 v1~v11 完整迭代；8-28 内置 7 只新自选股并接入 GitHub 备份。

---

## 1. 本次迭代主线

| 时间段 | 内容 |
|--------|------|
| 8-12 上午 | **扁平化 / Ins(iOS系统色) 风格改造**（用户反馈"字体太难看"） |
| 8-12 下午 | **价格提醒 v1** 上线（涨跌幅% + 目标价位，双通道通知） |
| 8-12 晚 | 价格提醒 v2~v6：猫头放大→QQ式跳动→差值文案→去虚线框→换透明猫头 |
| 8-13 上午 | v7~v11：差值 +/- 符号→猫头去圆框→"涨跌幅"标签→字号三档分级→popup 单行适配 |
| 8-13 上午 | 排查"价格提醒没反应" + 交互加固 |
| 8-28 上午 | **内置 7 只新自选股**（罗博特科/润禾材料/香农芯创/博杰股份/天孚通信/兆易创新/频准激光） |
| 8-28 上午 | **接入 GitHub 备份**（移除 server.js 硬编码 token，主分支 main，凭据管理器缓存） |
| 9-16 上午 | **行内「当日走势」迷你趋势线**（参考腾讯自选股）+ **代码下沉到名称下方**，行布局从 4 列改 5 列 |

---

## 2. 扁平化 / Ins 风格（定稿）

- **设计语言**：去渐变 / 去发光 / 去毛玻璃 / 去 text-shadow / 去字距
- **字体**：系统字体栈 `-apple-system, BlinkMacSystemFont, "Segoe UI", "Noto Sans SC", sans-serif` + 全局 `tabular-nums`
- **配色**（CSS 变量）：`--txt:#2C2C2E`（深灰）/ `--sub:#8E8E93` / `--sep:#E5E5EA` / `--hover:#F2F2F7` / `--accent:#C98A2B`(元宝金)
- **涨跌语义色（作用域分层）**：
  - 全局：`--up:#E05252`（红）、`--down:#3A3A3C`（**深灰**，主列表/详情弹窗负值低调）
  - 价格提醒窗口专用：`--alert-up:#E05252`、`--alert-down:#2A9D4A`（**唯一保留绿的位置**）
  - K线/资金流图：硬编码 `#1B7A3E`（中国股市惯例，保留）
- **图标**：全部内联线性 SVG（`.ic` 类），**禁止 emoji 作功能图标**（P0 规则，全量扫描=0）

---

## 3. 价格提醒功能（v11 最终态）

### 3.1 数据与检查链路
- localStorage key `moyu_price_alerts_v1`（独立于 CFG），结构 `{[code]:{pctOn,pctV,pctDir,priceOn,priceV,priceDir}}`
- 可提醒标的：自选股 + 黄金（`hf_GC` 现货黄金 / `domGold` 国内金价）
- 检查点：`render()` 末尾 `checkAlerts(rawTencent)` → 阈值判断 → `renderAlertBanner()` 横幅 + `notifyAlert()` 系统通知（tag 去重）
- 触发后横幅持续显示 + QQ式跳动（`alertHop 0.7s ease-in-out 0.42s infinite`），用户点 × 才停；价格回落自动重置

### 3.2 文案格式（tokens 结构，数字着色/文字黑色）
| 触发 | 文案 |
|------|------|
| 涨跌幅≥ | `涨跌幅 +X.XX% ≥ Y%`（红） |
| 涨跌幅≤ | `涨跌幅 -X.XX% ≤ -Y%`（绿） |
| 涨跌幅双向 | `涨跌幅 ±X.XX% 触及 ±Y%`（随方向着色） |
| 价位涨破 | `现价 X 高于目标 +Y 涨跌幅 ±Z%`（红/绿） |
| 价位跌破 | `现价 X 低于目标 -Y 涨跌幅 ±Z%`（红/绿） |

- `checkAlerts` 给 trigger 对象加 `kind`（'pct'/'price'），价格触发末尾才追加"涨跌幅"段（避免与涨跌幅触发 tokens 开头重复）

### 3.3 视觉（v11 最终）
- 无边框（`border:none`）+ 纯白底 + 浅阴影 `0 2px 8px rgba(0,0,0,0.07)`
- 猫头：40px 圆形透明 PNG（`jinyunabao.png` 内联），`border-radius:50%` 裁切
- 字号：`.ab-name` 15px / `.ab-txt` 9.5px / `.ab-num` 16.5px / gap 1px
- banner padding `10px 28px 10px 12px`（popup 356px 单行刚好容纳余 1px，溢出用 overflow:hidden 兜底）

### 3.4 排查结论与加固（8-13）
- 代码逻辑/语法/元素全部正常（Node 端到端模拟验证触发成功）
- "没反应"高概率是交互层：① 没点保存（已加固：勾选即自动保存+立即 refresh）；② 勾选但阈值无效（已加固：自动填 1%）；③ 宇树科技昨收 0 → pct 恒 0（涨跌幅提醒不适用，应设价位）；④ 通知未授权（横幅不依赖授权）

---

## 4. 自选股

- `DEFAULT_CONFIG.watch`（新用户）+ `NEW_WATCH` 兜底合并 IIFE（老用户 localStorage 自动去重 + unshift 置顶 + saveCfg）
- 收录：**宇树科技 `s_sh688836`**（科创板；昨收 0 → 涨跌幅提醒不适用，用价位）
- **2026-08-28 新增 7 只（截图内置）**：
  - 罗博特科 `s_sz300757`、润禾材料 `s_sz300727`、香农芯创 `s_sz300475`
  - 博杰股份 `s_sz002975`、天孚通信 `s_sz300394`、兆易创新 `s_sh603986`
  - **频准激光 `s_sh688826`**（⚠️ 截图 OCR 误读为 `688226`=威腾电气，实际代码差一位 8，须用 `688826`）
- 注意：主文件（`元宝爱财-摸鱼致富.html`）与 `上传版/原始版/index.html`、`上传版/趋势数据版/index.html` 三处均内置，保持一致。

---

## 5. 文件与运行（当前状态）

| 项 | 值 |
|----|----|
| 主文件 | `F:\摸鱼理财\元宝爱财-摸鱼致富.html`（~3.3MB 单文件） |
| 本地服务 | server.js 端口 18765（`http://localhost:18765/`） |
| **本地 index.html** | **必须与主文件同步**（server.js 优先服务 index.html；每次改主文件后 `cp` 同步） |
| 云端 | 阿里云 `/www/wwwroot/120.24.240.129/yuanbao/index.html`（宝塔拖覆盖 → 强刷） |
| 域名 | `yangyuting.cloud` 备案号 粤ICP备2026033457号-2 |
| **GitHub 仓库** | `yangyuting0304/yuanbao-financial-mannagement-tools`（主分支 `main`，已 git init + commit + push） |
| **上传版/** | 本地部署源目录（**已 gitignore 不进 GitHub**）：`趋势数据版/index.html`（当前主源）、`原始版/index.html`（8-04 旧基线，8-28 已补齐新股） |

**安全**：`server.js` 的 `JIN10_TOKEN` 已从硬编码改为 `process.env.JIN10_TOKEN || ""`，运行时用环境变量注入（本地 `set`/宝塔 environment），公开仓库不泄露。

**改动后必做**：改 `元宝爱财-摸鱼致富.html` → `cp` 到 `index.html` → `git add . && git commit && git push` → 本地强刷 → 上传云端（若用上传版则覆盖对应文件）。

---

## 6. 踩坑速查（本次新增）

- popup 356px 固定宽，单行内容 >316px 会溢出被裁 → 字号/gap 需按宽度精算
- `Object.assign({}, DEFAULT_CONFIG, parsed)` 浅合并 → localStorage 的 watch 数组会整体覆盖默认
- 腾讯对"昨收 0"的新股返回 pct=0 → 涨跌幅提醒永不触发
- 服务端优先服务 `index.html` 而非中文名主文件 → 改完必须同步，否则预览看到旧版
- **8-28 踩坑**：截图 OCR 把 `688826` 读成 `688226`，两者是不同科创板股（频准激光 vs 威腾电气）→ 名字与代码对不上时必须用腾讯/东财二次确认
- `上传版/` 被 `.gitignore` 排除 → 只作本地部署源，GitHub 备份不含此目录，云端同步需手动覆盖

---

## 7. 未来会话快速恢复

1. 读本文件 + `.workbuddy/memory/MEMORY.md`（权威长期上下文）
2. 需要细节 → 读 `.workbuddy/memory/2026-08-12.md` / `2026-08-13.md` / `2026-08-28.md` / `2026-09-16.md`
3. 当前未决：本地与云端需要用户手动上传同步；自选股/设置跨设备同步（二期可选）
4. GitHub 已关联：日常 `git add . && git commit -m "..." && git push` 即可备份（凭据管理器已缓存）

---

## 8. 行内「当日走势」迷你趋势线（2026-09-16 新增）

### 8.1 需求
参考腾讯自选股：① 自选股列表每行显示**当日分时迷你趋势线**，位置在**名称与价格之间**；② 股票代码**下沉到名称下方**、深灰淡显。

### 8.2 数据链路
- 腾讯 `web.ifzq.gtimg.cn/appstock/app/minute/query?code=sh601138`，返回每分钟一条 `"HHMM 价格 累计手 累计额"`
- ⚠️ **该接口不支持批量**（逗号传参 → `code param error`）→ 每只一个请求
- 支持 `sh/sz/hk` 股 + `hkHSI` + 指数；**不支持黄金 hf_GC**
- **新增服务端端点 `GET /api/mini?codes=...`**：`mapLimit()` 并发上限 6 → `MINI_CACHE` TTL 25s（失败也缓存 null）→ 降采样 ≤60 点 → 返回 `{p:[价格], n:原始点数, t1:"1033"}`。实测 9 只聚合 0.3s。
- 前端 `fetchSparklines()` 走 **30s 独立定时器**（不挤进 5s 行情心跳）；`render()` 顶部记录 `lastRenderArgs`，走势线到货后据此重绘。

### 8.3 渲染规则
- `viewBox="0 0 100 24"` + `preserveAspectRatio="none"` + `vector-effect="non-scaling-stroke"`
- **按昨收对称缩放**：`昨收 = 现价 − 涨跌额`（复用行内数据，天然一致）；`dev = max|p−昨收|`；`y = 12 − ((p−昨收)/dev) × 10.5`；**基准虚线固定在 y=12（居中）横贯全宽**（`opacity .38`、`dasharray 3 3`）
- 实线 x 铺到 `sessionFrac(t1) × 100`（A股 240min、港股 330min）→ 即"实线只画到当前时刻，右侧留白 + 虚线延伸"，与参考图一致
- 配色：涨 `#E05252` / 跌 `#2A9D4A`（沿用 App「图表涨红跌绿」惯例，**与主列表文字「跌=深灰」不同**）
- 全天不动兜底 `dev = base × 0.002` 防除 0

### 8.4 布局（4 列 → 5 列）
`名称 | 走势线 | 价格 | 涨跌额 | 涨跌幅%`
- **关键腾挪**：`.row.row-watch` 原固定 `padding-right:22px` 给删除按钮预留，但按钮只在编辑模式出现 → 改为仅 `#app.watch-edit` 时预留，平时收回 14px 给走势线
- 名称列仍锁 108px（保证长名称不截断）；走势线列实测约 59px（窄视口）/ 66px（宽视口）
- 名称列改纵向 flex：第一行 `.nm-text`，第二行 `.nm-sub`（代码 + 副标题 `<small>` 合并，**避免黄金行变三行**）

### 8.5 ⚠️ 关键坑
- `sparkCellHtml()` **必须始终输出 `.spark` 占位 span**（无数据留空），否则该行少一个子元素、后面 4 列整体左移错位。三处行构造（`rowHtml` / `watchRowHtml` / 黄金「获取中」占位行）都已插入。
- 想更醒目：当前按真实时间轴铺开（10:30 约 27%），改 `sparkSvgInner()` 里 `xEnd` 为 `W` 即可铺满全宽（一行）。
