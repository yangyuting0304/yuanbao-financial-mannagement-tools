# 上传说明（2026-09-16 行内走势线版）

> ⚠️ **与以往不同：这次必须连 `server.js` 一起更新并重启服务**，因为走势线依赖新增的 `/api/mini` 接口。
> 只传 `index.html` 的话，页面能打开但**走势线一格会是空白**。

## 一、要上传的文件（2 个）

| 本地文件 | 上传到云端位置 |
|----------|----------------|
| `部署包/index.html` | `/www/wwwroot/120.24.240.129/yuanbao/index.html` |
| `部署包/server.js` | `/www/wwwroot/120.24.240.129/yuanbao/server.js` |

（宝塔 → 文件 → 进入该目录 → 把两个文件拖上去覆盖）

## 二、上传后必须重启服务（新要求）

以前改 HTML 不用重启，这次改了 `server.js`，必须重启 Supervisor 里的 `yuanbao` 进程：

- **宝塔图形界面**：软件商店 → 已安装 → Supervisor管理器 → 找到 `yuanbao` → 重启
- **或 SSH 终端一条命令**：
  ```bash
  supervisorctl restart yuanbao
  ```

## 三、上传前建议先备份旧 server.js（可回滚）

宝塔文件管理里对 `server.js` 右键 → 复制 → 粘贴为 `server.js.bak-0916`。
万一新版有问题，把备份改名回 `server.js` 再重启即可回滚。

## 四、验证（3 步）

```bash
# 1. 新接口是否生效（应返回 JSON，含 "ok":true 和 p:[...] 价格数组）
curl "http://120.24.240.129/yuanbao/api/mini?codes=s_sh601138,s_sz002975"

# 2. 旧接口没被弄坏（应返回 JSON 且 source 字段正常）
curl "http://120.24.240.129/yuanbao/api/domgold"

# 3. 手机/电脑打开 http://120.24.240.129/yuanbao/ 强刷（Ctrl+F5）
#    → 自选股每行名称右侧出现当日走势迷你线，代码在名称下方灰色小字
```

## 五、已知情况（非本次引入）

云端目前金十数据源走的是**兜底**（国际金价=腾讯 COMEX、国内金价=招行 SGE），
说明云端的 `JIN10_TOKEN` 环境变量未生效或已失效。若想恢复金十主源：
宝塔 → Supervisor管理器 → yuanbao → 配置文件，检查
`environment=JIN10_TOKEN="你的金十token"` 是否填了真实 token，改完重启。

不处理也不影响本次更新——覆盖 `server.js` 后金十行为与现在完全一致（没有回归）。

## 六、安全提醒

本次 `server.js` **不含任何硬编码 token**（金十 token 走环境变量），
可以直接上传，不会泄露到 GitHub（部署包已入库但 token 本来就不在代码里）。
